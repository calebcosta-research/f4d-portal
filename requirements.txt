streamlit==1.58.0
pandas==2.2.3
plotly==6.7.0
matplotlib==3.10.9
SQLAlchemy==2.0.50
python-dotenv==1.2.2
psycopg2-binary==2.9.9
openpyxl==3.1.5
azure-storage-blob==12.23.1
pyodbc==5.2.0
# pyodbc is the driver the original F4D app used on this Connect (ODBC Driver 17
# for SQL Server), so the Connect build environment supports it. pymssql==2.3.1
# is a self-contained fallback if a build ever lacks the system ODBC libraries.
